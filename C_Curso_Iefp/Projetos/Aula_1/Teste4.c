#include <stdio.h>

int main()
{

    int n, o, p, soma;

    n = 7;
    o = 8;
    p = 1376;
    soma = n + o + p;
    double junior;

    printf("%i", soma);

    printf("Vamos somar variaveis no print:\n");

    printf("O valor de n(%i) + o(%i) + p(%i) e igual a soma(%i).\n",n,o, p, (n+o+p));

   printf("%d", junior);
}